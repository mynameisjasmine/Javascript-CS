/ Big O of Array Operations */


// acessing a specific index in an array is constant time (O1)
// Pushing (adding to the end of an array) is (O1)
// Adding to the beginning of an array is O(n) - this is best to avoid if you can
// Push and Pop are always faster than Shift and Unshift
// Searching an array is O(n)


/* Built In Array Methods */

// push - O(1)
// pop - O(1)
// shift - O(n)
// unshift - O(n)
// concat - O(n)
// slice - O(n)
// splice - O(n)
// sort - O(n * log n)
// forEach/map/filter/reduce/etc - O(n)
